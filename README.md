# The Daily Quest public site

Static support and privacy pages. No build dependencies or tracking. Deploy the repository root with GitHub Pages.

Cloudflare alternative: connect this repository to Cloudflare Pages, leave the build command empty and publish the root directory. Set the public URLs in the app and store listings after verifying the new host. The app source remains in its separate private repository. A custom domain is optional.
